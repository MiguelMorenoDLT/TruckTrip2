<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.unipiloto.trucktrip2">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TruckTrip2">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:exported="false"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".StatusActivityActivity"
            android:exported="false" />
        <activity
            android:name=".LocationActivityActivity"
            android:exported="false"
            android:label="@string/title_activity_location_activity"
            android:theme="@style/Theme.TruckTrip2.NoActionBar" />
        <activity
            android:name=".Driver"
            android:exported="true" />
        <activity
            android:name=".TruckManager"
            android:exported="true" />
        <activity
            android:name=".TripsDriver"
            android:exported="true" />
        <activity
            android:name=".See_transports"
            android:exported="true" />
        <activity
            android:name=".Register_Truck"
            android:exported="true" />
        <activity
            android:name=".View_Loadout"
            android:exported="true" />
        <activity
            android:name=".Loadout"
            android:exported="true" />
        <activity
            android:name=".CreateLoad"
            android:exported="true" />
        <activity
            android:name=".RegistrationActivity"
            android:exported="true" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>